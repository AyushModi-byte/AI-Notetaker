/**
 * README
 * ------
 * Before running this app on your physical iPhone via Expo Go, replace the
 * `API_BASE_URL` placeholder below with your computer's LAN IP address
 * (e.g. "http://192.168.1.42:8000"). `localhost` will NOT work from a
 * physical device — it only works in an iOS Simulator / Android Emulator
 * running on the same machine as the backend.
 *
 * Find your LAN IP:
 *   - Windows: run `ipconfig` and look for "IPv4 Address" under your Wi-Fi adapter.
 *   - Mac/Linux: run `ifconfig` (or `ipconfig getifaddr en0` on Mac).
 * Your phone and computer must be on the same Wi-Fi network.
 */

// EDIT ME: replace with your machine's LAN IP (see README comment above).
export const API_BASE_URL = 'http://10.144.40.179:8000';
